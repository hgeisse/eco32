#
# Makefile for standalone library
#

BUILD = ../build

all:

install:
		mkdir -p $(BUILD)/include
		cp ../lcc/include/eco32/linux/* $(BUILD)/include
		mkdir -p $(BUILD)/lib
		cp ./scripts/* $(BUILD)/lib

clean:
		rm -f *~
